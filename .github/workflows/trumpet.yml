name: blank

on:
  push:

jobs:
  calculator:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        chart: ["schedule", "triumph", "slice", "restore", "drama"]
        ingredient: ["mark", "invitation", "curl", "exert"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java -jar Te
